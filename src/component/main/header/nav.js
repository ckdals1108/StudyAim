import React from 'react';
import styles from './nav.module.css';

const Nav = () => {
    return (
        <div className={styles.container}>
            네비게이션
        </div>
    );
};

export default Nav;